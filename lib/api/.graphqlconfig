{
  "name": "Climate Platform GraphQL Schema",
  "schemaPath": "schema.graphql",
  "extensions": {
    "endpoints": {
      "AppSync": {
        "url": "${env:APP_SYNC_API_URL}",
        "headers": {
          "x-api-key": "${env:APP_SYNC_API_KEY}"
        },
        "introspect": true
      }
    }
  }
}