query Data($database: ID!, $limit: Int, $offset: Int) {
    list(database: $database) {
        total
        slice(limit: $limit, offset: $offset) {
            usedLimit
            usedOffset
            entities {
                ...Entity
            }
        }
    }
}