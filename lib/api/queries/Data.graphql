query Data($database: ID!) {
    list(database: $database) {
        total
        slice {
            usedLimit
            usedOffset
            entities {
                ...Entity
            }
        }
    }
}