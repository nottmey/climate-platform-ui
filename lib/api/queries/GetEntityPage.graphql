query GetEntityPage($database: ID!, $page: Int, $size: Int, $filter: EntityFilter) {
    list(database: $database, filter: $filter) {
        page(page: $page, size: $size) {
            info {
                ...PageInfo
            }
            entities {
                ...Entity
            }
        }
    }
}